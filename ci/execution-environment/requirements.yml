---
# Collections baked into the Execution Environment.
#
# axonops.axonops is installed from an explicit git tag rather than from Galaxy
# so that the image can be built for a tag before (or without) a Galaxy release.
#
# This pin records what the last released image contains. It is maintained by
# the release workflow: pushing a `v*` tag builds the image from that tag and
# commits the matching pin back to the default branch, so no manual bump is
# needed. Manual `workflow_dispatch` builds override it with the ref they are
# given, and pull-request builds use `main`.
collections:
  - name: https://github.com/axonops/axonops-ansible-collection.git
    type: git
    version: v0.6.3
